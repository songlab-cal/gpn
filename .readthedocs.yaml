version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  jobs:
    pre_build:
      - python docs/prepare_notebooks.py

python:
  install:
    - method: uv
      command: sync
      groups:
        - docs

sphinx:
  builder: dirhtml
  configuration: docs/conf.py
  fail_on_warning: true
